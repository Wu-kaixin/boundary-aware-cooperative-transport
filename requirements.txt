# DBACT dependencies.
#
#   pip install -r requirements.txt
#   pip install -e .
#
# Or use the conda environment, which is what the acceptance commands assume:
#   conda env create -f environment.yml && conda activate dbact
#
# Python >= 3.10.

# --- core runtime ---
numpy>=1.26
scipy>=1.13
matplotlib>=3.9
PyYAML>=6.0

# --- QP backends ---
# The default 'qp' backend is the exact planar solver in dbact/qp2d.py and needs
# nothing here. cvxpy is the independent cross-check backend used by
# tests/test_qp2d.py; the C2 contract makes a requested backend fail rather than
# silently fall back, so this is a required dependency of the paper pipeline.
cvxpy>=1.4
osqp>=0.6

# --- physics ---
# pymunk is the independent rigid-body engine used to confirm that the transport
# results are not an artefact of the penalty contact model.
pymunk>=6.6
shapely>=2.0

# --- analysis ---
pandas>=2.2

# --- publication rendering ---
# The traceable publication package always emits H.264 MP4 files.  Matplotlib
# does not bundle an encoder; imageio-ffmpeg supplies a pinned executable path.
imageio-ffmpeg>=0.6

# --- development ---
pytest>=8.0

# DBACT full-framework dependencies
# Covers: src/dbact, src/dbact_sim, src/mas_adapter, scripts, and tests
#
# Quick install:
#   pip install -r requirements.txt
#   pip install -e .          # editable package (recommended)
#
# Python: >=3.9 (3.10+ recommended)

# --- Core runtime (required by all layers) ---
numpy>=1.26
matplotlib>=3.9
PyYAML>=6.0

# --- Simulation & geometry extensions ---
scipy>=1.13
shapely>=2.0

# --- Formal CBF-QP solver (Stage 2 roadmap; current default uses lightweight projection) ---
cvxpy>=1.4
osqp>=0.6

# --- Metrics, trajectories, experiment analysis ---
pandas>=2.2

# --- Development & testing ---
pytest>=8.0

# --- Original baseline reproduction (local Voronoi / legacy QP backends) ---
geovoronoi>=0.2.0
cvxopt>=1.3.3

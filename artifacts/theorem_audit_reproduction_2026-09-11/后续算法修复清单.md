# 后续算法修复清单（本轮未实施）

本轮只做冻结提交 `98ba28e` 的复现与核查。下列工作需要改控制器、执行模型或证明接口，**不要**在复现分支上用调参去对齐压缩包数字。

## P0 执行接口（先于扩写证明）

1. 采用同步采样模型：`U_k = K(P_k, M_k, z_k)`，`P(t_k+τ)=P_k+τ U_k`，`0≤τ≤Δ`。在配置与代码中写清感知、地图、决策、QP、执行的顺序。
2. 对最终执行速度加入墙约束，保证未裁剪的一步更新仍留在 `D` 内。
3. 取消常规位置裁剪；越界或不可行记为运行失败，不得投影后继续称为获证轨迹。
4. 每步检查最终 `U_k` 的机器人间半约束、速度界；遗漏远邻要有半径证书。
5. 单独的 `theorem_mode`：固定几何、固定偏移、固定参考密度；搜索/重部署/搬运模式要么纳入证明，要么明确排除。

## P1 与 Assumption 5 / 网格不连续

6. 当前 `LocalCVT.cell_samples` 硬圆盘 + 硬 Voronoi 归属，以及 `restrict` 硬删高斯原子，不能当作全域局部 Lipschitz 连续时间反馈。
7. 仅把网格从 20 提到 80 不会消除硬切换。若坚持连续 ODE 路线，需要连续积分/平滑接口，并证明最终 QP 映射的正则性。
8. 若改走采样模型，则不要再声称 Assumption 5 已被当前 endpoint-grid 满足。

## P2 安全与 Assumption 2

9. 不能把 `max_object_speed: 0.25` 当作真实物体平移/旋转上界。seeds 5/8 的 `max_cargo_speed` 已超过 0.25。
10. `range_noise_std` 是高斯标准差，不能直接当作确定性 `ε_b`。
11. 物体 barrier scaling 保留原名；seed 2 的 3 次缩放不得删除或改判为通过。

## P3 理论界

12. 不要把静态 `B_{g,∞}≈18963` 写成动态仿真保证；运动项只会增大右端。
13. 不要把论文右端改成 `min(理论界, 几何界)` 来粉饰。几何界 `4R²M_tot²≈8.986` 已严格支配粗界。
14. 候选紧界方向：保留饱和命令的下降方向；避免先用全局最差 `m_-` 替换局部质量；把采样余项写出；分解地图/积分/偏置/饱和/安全修正/`∂tH`。
15. 若候选界仍被几何界支配，应收缩论文主张，而不是加长当前证明。

## P4 局部稳定性

16. Lemma 4 / Proposition 3 / Corollary 1 暂留为条件结果。
17. 恢复为核心结论前：指定 N=16 候选 `P★`、带误差界的驻点、完整 Hessian、正的最小特征值下界、邻域在 `int(S)` 内，并核验 `d̄ < a_- μ r`。
18. 若证书仍得不到，从主文移除这组局部稳定性承诺。

## P5 下一轮实验（修复后再跑）

19. 静态准确地图、静态局部感知、有运动上界的移动参考，各含圆/矩形/L，每形 5 个预定种子。
20. 墙角/拥挤压力测试记录每步半约束、墙约束、持有段最小间距、失败事件。
21. 代表案例比较 `Δ=0.05/0.025/0.0125` 与网格 `20/40/80`，保持相同物理时长。
22. 每个控制时刻导出 `P`、物体位姿、`H★, m_i★, c_i★, g_i`、局部地图质心、最终 `U`、模式、误差分量、barrier、clip/reset、QP 状态。SEARCH/APPROACH 缺失 CVT 不得填零。
